---
description: HalalScan agent checklist — architecture, scope; DoD is DEFINITION_OF_DONE.md only
alwaysApply: true
---

# Agent checklist (HalalScan)

**Definition of done:** follow **[DEFINITION_OF_DONE.md](../../DEFINITION_OF_DONE.md)** only — do not duplicate its steps in replies or other docs.

- **During work:** items 1–3 after non-trivial code changes.
- **When user says task done:** full **Task done** section in that file.

## Architecture (read `CLAUDE.md` for detail)

- Lookup: test DB → local cache → Supabase edge function → OpenFoodFacts + keywords.
- Halal layers: optional AI (Claude/Gemini when enabled) → keywords → **keyword safety override** (must not be weakened). **Claude is off by default** in this repo (`CLAUDE_ENABLED=false`).
- Halal logic: `lib/` and `supabase/`; avoid `android/`/`ios/` unless platform task.
- UI/scan/result changes: see `ui-e2e` rule. Supabase edits: see `supabase` rule.

## Scope

- Minimal diff; match local naming and structure.
- Never commit or push unless the user explicitly asks.
- For multi-file or architectural changes, prefer Plan mode first, then implement.

## Vault (plans & product docs)

- **Implementation plans and feature specs** → Obsidian vault `01 - Product/Features/` (not `halal_checker/docs/`).
- Vault path: `C:\Users\Hamza\Documents\Pers\AI\obsidian\halalscan\`
- **Decisions** → `01 - Product/Decisions/`; link Asana + [[Roadmap]] when relevant.
- See vault `CLAUDE.md` for debrief, bugs, release log.
