tags:
  manual:
    skip: "Manual test — requires a connected device and test asset images. Run with: flutter test integration_test/ocr_pipeline_test.dart"
  e2e:
    skip: "UI E2E — requires a connected device/emulator. Run with: .\\run_ui_e2e_test.ps1 or ./run_ui_e2e_test.sh"
