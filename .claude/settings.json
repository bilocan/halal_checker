{
  "permissions": {
    "allow": [
      "Bash(python3 -c ' *)",
      "Bash(xargs head *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json; d=json.load(sys.stdin); print(d.get('tool_input',{}).get('file_path',''))\" | { read -r f; echo \"$f\" | grep -qE '\\.dart$' && dart format \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Formatting Dart file..."
          }
        ]
      }
    ]
  }
}
